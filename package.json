{
  "name": "project-3-travva-app",
  "scripts": {
    "start": "node ./server/server.js",
    "postinstall": "cd client && npm i && npm run build && cd .. & cd server && npm i && cd .."
  },
  "dependencies": {
    "express-session": "^1.17.1"
  }
}
